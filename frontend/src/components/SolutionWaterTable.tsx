import {WaterUi} from "../model";


import * as React from "react";
import {Component} from "react";

interface WProps {
    waters: Array<WaterUi>
}

export class SolutionWaterTable extends Component<WProps, {}> {
    render() {
        return (<>
        </>)
    }
}